# 使用surfpool的时候，因为vpn问题导致端口冲突

使用surfpool的时候，因为vpn问题导致端口冲突

由于surfpool需要同步远程节点数据，所以必须开vpn。但是本地使用surfpool的时候，请求又会被路由到vpn端口。

解决办法是在使用solana命令时添加 NO_PROXY="127.0.0.1,localhost"，这样本地流量不会路由到vpn端口了，直达本地surfpool节点
